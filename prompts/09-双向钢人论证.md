# 双向钢人论证
> 场景:决策 | 适用:两个选项都有道理不知道选哪个,正反各给最强论证再找翻盘变量

---

**这一条还没有正文,需要你自己填。**

本仓库只带索引和路由,不转载任何人的 prompt 正文。「双向钢人论证」的原文出自数字生命卡兹克的 X Article
《都Agent时代了,我还是想分享给你这12个我最常用的Prompt。》— https://x.com/Khazix0918/status/2090726395035156964

填法(三步,一次就好):

1. 打开上面的原文,找到对应的那一条;
2. 把它的正文粘到本文件 `---` 这条横线**下面**,连同这段说明一起替换掉;
3. 需要你每次填的地方,写成 `【这样】`。`/pp` 会拿你当轮给的信息自动填进去,填不了的会先问你。

不想用原文也行:整条换成你自己的 prompt,把上面两行的名称和场景描述一并改掉即可。
`/pp` 认的是文件本身,不认编号——你也可以直接删掉这个文件。

> 提示:改这里只对**本机这一份**生效,插件升级会被覆盖。要长期保存,把填好的文件放到
> `~/.claude/prompts/` 下(同名即可,`/pp` 优先用你那份)。
